# 会议纪要｜核电工件数据采集

## 一、结论
1. **相机与图像类型**：统一使用 **RGB 彩色**数据（灰度图不足以支撑算法）；优先在现有“图样”环境与设备上开展采集与验证。  
2. **采集覆盖**：围绕工件 **7 个大角度**（含顶视）采集；每个大角度 **30 张**，合计 **210 张/型号**。每张需有**小幅扰动**（平移/旋转/俯仰/相机末端角），避免重复。  
3. **光照/场景多样性**：至少三种可量化环境：正常光、暗光、轻度干扰（常见工具等），同一机位优先**快速切换环境**提升效率。  
4. **标注策略**：以**矩形框（目标检测）**为主，统一**型号命名与唯一 ID**。
5. **目录与命名**：按**数据类型归档**：`RGB/Depth/Pose/PointCloud` 子目录；文件名**序号对齐**（如 `001.jpg ↔ 001.png ↔ 001.txt`），便于匹配检索。  
7. **CAD建模** ：新增建立**7 个型号**的 CAD/STL 模型的需求，以支持三维重建与对齐算法。

---

## 二、数据采集规范

### 1) 拍摄位姿与张数
- **大角度（7 个）**：环绕工件 6 个周向位姿 + 1 个顶视位姿；周向位姿尽量**均匀环绕**。  
- **每大角度 30 张**：围绕该大角度做**小扰动**（前/后/左/右小量平移、俯仰少量变化、相机末端少量自转），保证每张信息量不同；避免同角度同光照多连拍的冗余。

### 2) 光照与场景
- **正常光**：开灯、窗帘开启（实验室日常）。  
- **暗光**：关灯并拉上遮光帘（近黑）。  
- **干扰场景**：台面摆放常见工具/零部件，模拟现场杂散物（按项目阶段决定是否启用）。  
- **效率建议**：同一机位按“正常光 → 暗光 → 干扰”顺序**切环境不动机位**，提升采样效率。

## 