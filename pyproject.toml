[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aiws-alignment-feat-model-free"
version = "0.1.0"
description = "AIWS fine alignment and weld extraction package"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["aiws_alignment_feat_model_free*"]

